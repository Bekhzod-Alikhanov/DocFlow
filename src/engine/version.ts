/**
 * Model version. Bump whenever an equation, default parameter, or the parameter
 * registry changes in a way that alters trajectories. Recorded in every RunRecord
 * and stamped onto every export, so a saved scenario is always traceable to the
 * math that produced it (spec §3.8, §4.3).
 */
export const MODEL_VERSION = '0.2.0'
